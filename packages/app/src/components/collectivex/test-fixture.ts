/**
 * Re-export of the CollectiveX contract fixture builders, which live next to
 * the shared reader in the db package. Kept at this path so component tests
 * and cypress specs import fixtures the same way as the rest of this folder.
 */

export * from '@semianalysisai/inferencex-db/collectivex/test-fixture';
